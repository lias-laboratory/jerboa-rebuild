\section{Parametric specifications}

\input{../tables/parametric-specification-table}

The reevaluation of an object first requires the recording of its construction
({\color{blue}modelling}) process in the form of a parametric specification. %
The production of such a record is a two-part task, namely the persistent
identification of darts and the persistent naming of topological parameters. %
In such a record, an entry must comprise an application ordering number
(\emph{AppNumber}), a name (\emph{Name}), topological parameters {\color{blue}as
  persistent names} (\emph{TopoParameters}) and geometric parameters
(\emph{GeoParameters}) such as follow
%
\begin{equation*}
  \begin{split}
    [&AppNumber, Name, TopoParameters,\\ &GeoParameters]
  \end{split}
\end{equation*}
%

\subsection{Persistent ID}

% What is a PI ?
A \emph{Persistent ID} (or \emph{PI}) is a data structure which is part of the
persistent naming scheme proposed in this contribution. %
This structure identifies a dart by recording all the rules that affected it
during a construction. %
A PI must also be unique in order to avoid ambiguity in a Persistent Name. %

% Why is a PI needed ?
As suggested in Section~\ref{ssec:g-maps}, a dart is the topological entity of
lowest dimension in a G-map and is part of several orbits. %
Since darts result from the application of rules, their usage in a persistent
naming scheme means they must be persistently identified.%
%
% How PIs are built ?
In order to describe the state of a dart at any time during a construction, a PI
records an \textit{AppNumber} and a node name (\emph{NodeName}). %
Upon the application of a rule, although a node matches one or more darts, a
dart is matched by at most one node. %
Thus, for each application that affected a dart $d$, a PI must record the name
of the node that matched $d$ such that
%
\begin{equation*}
  \begin{split}
    PI_{d} = [AppNumber &- NodeName;\\ AppNumber &- NodeName;\\ &\dots\\
    AppNumber &- NodeName]
  \end{split}
\end{equation*}
%
is a syntactic example of a PI associated to $d$. %

% How can a PI be unique ?
A PI, by construction, is unique. %
When a rule has an empty left member, each node creates exactly one dart and
each of the associated PI is unique. %
{\color{darkgreen}For example, the create-square rule listed in
  Table~\ref{tbl:param-spec-example} has no left member. %
  When the rule is applied, each of its nodes creates a dart such that each one
  has a unique PI (\textit{i.e.}, $PI_{d} = [1n_{0}]$, $PI_{d'} = [1n_{1}]$,
  $\dots$, $PI_{d''} = [1n_{7}]$). %
} %

From this point on, any future PI will either be an update or a creation derived
from the PI of an already existing dart. %
And because of the former PIs being unique, any subsequent PI is unique. %
{\color{darkgreen}For example, the extrude-face rule, also listed in
  Table~\ref{tbl:param-spec-example}, has one node in its left member and six in
  its right member. %
  With this rule, the darts matched by the left member (preserved by the node
  $n_{0}$ in the right member) are updated from $[1n_{0}]$, $[1n_{1}]$, $\dots$,
  $[1n_{7}]$ to $[1n_{0} - 2n_{0}]$, $[1n_{1} - 2n_{0}]$, $\dots$,
  $[1n_{7} - 2n_{0}]$ and each PI remains unique. %
  The same goes for the newly created darts, depending of the node that creates
  them. %
}


\subsection{Persistent Name}

% What is a PN?
A \emph{Persistent Name} (or \emph{PN}), is a structure which identifies, with a
collection of PIs, an orbit designated as a topological parameter. %

% Why is a PN needed ?
Although PIs can persistently identify darts, a dart is only a part of an
orbit. %
When two operations affect two parts of an orbit which do not share any dart,
some changes to the orbit may be lost in the record if only one PI is
considered. %

% How a PN is built?
In such a case, a PN will collect one or more PIs to precisely describe an orbit
as a topological parameter such that
%
\begin{equation*}
  PN_{a} = \{PI_{x} \dots PI_{z}\}
\end{equation*}
%
is a syntactic example of a PN. %

\begin{figure*}[ht!]
  \centering
  \begin{subfigure}[t]{0.119\textwidth}
    \includegraphics[width=\textwidth]{house-ground}
    \caption{}
    \label{fig:house-ground}
  \end{subfigure}
  \begin{subfigure}[t]{0.119\textwidth}
    \includegraphics[width=\textwidth]{house-walls}
    \caption{}
    \label{fig:house-walls}
  \end{subfigure}
  \begin{subfigure}[t]{0.119\textwidth}
    \includegraphics[width=\textwidth]{house-reshape-flat-roof}
    \caption{}
    \label{fig:house-reshape-flat-roof}
  \end{subfigure}
  \begin{subfigure}[t]{0.119\textwidth}
    \includegraphics[width=\textwidth]{house-higher-flat-roof}
    \caption{}
    \label{fig:house-higher-flat-roof}
  \end{subfigure}
  \begin{subfigure}[t]{0.119\textwidth}
    \includegraphics[width=\textwidth]{house-triangulate-facade}
    \caption{}
    \label{fig:house-triangulate-facade}
  \end{subfigure}
  \begin{subfigure}[t]{0.119\textwidth}
    \includegraphics[width=\textwidth]{house-reshape-flat-to-sharp-roof}
    \caption{}
    \label{fig:house-reshape-flat-to-sharp-roof}
  \end{subfigure}
  \begin{subfigure}[t]{0.119\textwidth}
    \includegraphics[width=\textwidth]{house-flatten-sharp-roof}
    \caption{}
    \label{fig:house-flatten-sharp-roof}
  \end{subfigure}
  \begin{subfigure}[t]{0.119\textwidth}
    \includegraphics[width=\textwidth]{house-color-facade}
    \caption{}
    \label{fig:house-color-facade}
  \end{subfigure}
  \caption{House modelling process}
  \label{house-color-facade}
\end{figure*}

% How much PIs are necessary to persistently describe an orbit?
In order to precisely describe an orbit, a PN must gather a necessary and
sufficient number of PIs. %
Since the rules that affected an orbit may not all be recorded in a single PI,
then a PN must collect PIs such that a maximum of rules are represented. %
To avoid redundancy, and make a PN easier to read, it is possible to prune PIs
such that a minimum of PIs remains, provided that there is always at least one
occurrence of each rule related to the orbit. %
{\color{darkgreen}For example, the Table~\ref{tbl:param-spec-example} eighth
  entry identifies a face coloured in blue during the modelling process
  (Fig.~\ref{fig:house-color-facade}). %
  This face is the result of three rule applications namely the creation of a
  face, the extrusion of this face and finally the insertion of a vertex. %
  Therefore, its PN must gather PIs in order to describe the applications theses
  three rules. %
  Since the only PI within $PN_{7}$ holds an occurrence of AppNumbers one, two
  and three in its record, no other PI is needed. %
%
}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../main"
%%% End:
