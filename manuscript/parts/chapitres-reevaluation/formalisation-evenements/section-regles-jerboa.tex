\section{Les évènements dans les règles Jerboa }
\label{sec:formalisation-evenement-regles-jerboa}

\highlight{chapeau}

\subsection{Suivi des orbites lors des applications des règles}
\label{sec:formalisation-evenements-regles-jerboa-suivi}

%\maxime { adapter les propositions aux schémas: wip}

\subsubsection{Création}

\begin{figure}[htbp!]
	\centering
	\includegraphics[height=50pt]{rule-face-triangulation-3D-no-color}
	\caption{Règle de triangulation de face}
	\label{fig:formalisation-evenement-regles-jerboa-regle-triangulation}
\end{figure}

% La figure \textbf{fig:rule-insert-vertex} représente une règle d'insertion
% d'un sommet sur une arête où les nœuds $n_2$ et $n_3$ (en rouge) dans $R$
% filtrent l'orbite du sommet créé. \maxime{à refaire}

Continuons avec la règle de triangulation, cette fois sous la forme d'une règle
Jerboa
(Figure~\ref{fig:formalisation-evenement-regles-jerboa-regle-triangulation}). %
Comme défini dans la section précédente un sommet est créé par règle de
transformation de graphe. %
Nous exprimons maintenant la création du sommet $R\orbit{1,2,3}(n2)$ dans $H$.

\begin{proposition}[Création d'orbite]
	Soit une règle $r~: L \longrightarrow R$, un nœud d'accroche $h$ de $L$, un morphisme de
	filtrage $\underline{m}~: \underline{L} \rightarrow G$ sur une G-carte $G$, et un type
	d'orbite $\orbit{\omega}$.

	Soit $v$ un nœud de $R$. L'orbite $\underline{R}\orbit{\omega}(v)$ est créé dans la
	règle si et seulement si toutes les orbites $G\orbit{\omega}(m((v,v')))$ sont créés
	dans la transformation directe $G \xlongrightarrow{r, \underline{m}} H$, pour tous les nœuds
	$v'$ de l'orbite d'instanciation $G\orbit{l(h)}(\underline{m}(h))$.
\end{proposition}

\begin{proof}
	Soit $v$ un nœud de $R$ tel que l'orbite $\underline{R}\orbit{\omega}(v)$ est créée
	par la règle, c'est-à-dire tel que
	$\underline{R}\orbit{\omega}(v) \cap \underline{L} = \emptyset$. Soit
	$O = G\orbit{l(h)}(\underline{m}(h))$, l'orbite d'instanciation.

	Soit $w$ un nœud quelconque de l'orbite $\underline{R}\orbit{\omega}(v)$. $w$ est un
	nœud créé de la règle de $R \setminus L$. Donc, par construction de la règle instanciée,
	tous les nœuds $(w,v')$ pour tous les nœuds $v'$ de l'orbite d'instanciation
	$O$, sont des nœuds créés dans la règle instanciée $r(\underline{m})$.

	Donc toutes les orbites $R(O)\orbit{\omega}((v,v'))$ pour tous les nœuds $v'$ de
	l'orbite d'instanciation $O$ sont des orbites créées dans la règle instanciée
	$r(\underline{m})$. Et donc, d'après la proposition \ref{prop:base-rule-create},
	toutes les orbites $G\orbit{\omega}(m((v,v')))$ sont créés dans la transformation
	directe $G \xlongrightarrow{r, \underline{m}} H$, pour tous les nœuds $v'$ de l'orbite
	d'instanciation $O$.
	\\

	Réciproquement, supposons que toutes les orbites $H\orbit{\omega}(m'((v,v')))$ sont créés dans la transformation directe $G \xlongrightarrow{r, \underline{m}} H$, pour tous les nœuds $v'$ de l'orbite d'instanciation $O$.
	Donc, d'après la proposition \ref{prop:base-rule-create}, toutes les orbites $R(O)\orbit{\omega}((v,v'))$ pour tous les nœuds $v'$ de l'orbite d'instanciation $O$ sont des orbites créées dans la règle instanciée $r(\underline{m})$.
	Donc par construction de l'instanciation, $\underline{R}\orbit{\omega}(v)$ est une orbite créée dans la règle $r$.
\end{proof}

\maxime{Instanciation + description}

Lorsque $L \neq \emptyset$, une orbite créée l'est à partir du renommage d'une orbite
incidente au nœud d'accroche. Par exemple la règle d'insertion de sommet figure
\textbf{fig:rule-insert-vertex} crée un sommet dont la topologie dépend de celle
de l'arête d'origine. Dans l'application figure
\textbf{fig:colored-rule-insert-vertex}, l'arête incidente à un seul cube comprend
quatre brins ainsi le sommet créé a aussi quatre brins. Si l'arête sur laquelle
on applique la règle est incidente à deux cubes, le sommet aura deux fois plus
de brins.

\subsubsection{Suppression}

\begin{figure}[ht]
	\centering
	\includegraphics[height=50pt]{rule-dissolve-vertex-in-face-3D-no-color}
	\caption{Règle de dissolution de sommet dans une face}
	\label{fig:formalisation-evenement-regles-jerboa-regle-dissolution-sommet}
\end{figure}


% La figure \textbf{fig:inst-rule-isolated-face-delete} représente une règle de
% suppression de face isolée où le nœud $n0$ dans $L$ filtre l'orbite de l'arête
% supprimée.

Reprenons la règle de dissolution d'un sommet dans une face
(Figure~\ref{fig:formalisation-evenement-regles-jerboa-regle-dissolution-sommet}). %
Comme défini dans la section précédente, un sommet est supprimé par la règle de
transformation de graphe. %
Nous exprimons maintenant la suppression du sommet $L\orbit{1,2,3}(n2)$ dans
$H$. %

\begin{proposition}[Suppression d'orbite]
	Soit une règle $r~: L \longrightarrow R$, un nœud d'accroche $h$ de $L$, un morphisme de
	filtrage $\underline{m}~: \underline{L} \rightarrow G$ sur une G-carte $G$, et un type
	d'orbite $\orbit{\omega}$.

	Soit $v$ un nœud de $L$. L'orbite $\underline{L}\orbit{\omega}(v)$ est supprimée
	dans la règle si et seulement si toutes les orbites $G\orbit{\omega}(m((v,v')))$
	sont supprimées dans la transformation directe $G \xlongrightarrow{r,\underline{m}} H$, pour
	tous les nœuds $v'$ de l'orbite d'instanciation
	$G\orbit{l(h)}(\underline{m}(h))$.
\end{proposition}

\begin{proof}
	Soit $v$ un nœud de $L$ tel que l'orbite $\underline{L}\orbit{\omega}(v)$ est
	supprimée par la règle, c'est-à-dire tel que
	$\underline{L}\orbit{\omega}(v) \cap \underline{R} = \emptyset$. Soit
	$O = G\orbit{l(h)}(\underline{m}(h))$, l'orbite d'instanciation.

	Soit $w$ un nœud quelconque de l'orbite $\underline{L}\orbit{\omega}(v)$. $w$ est
	un nœud supprimé de la règle de $L \setminus R$. Donc par construction de la règle
	instanciée, tous les nœuds $(w,v')$ pour tous les nœuds $v'$ de l'orbite
	d'instanciation $O$, sont des nœuds supprimés dans la règle instanciée
	$r(\underline{m})$.

	Donc toutes les orbites $L(O)\orbit{\omega}((v,v'))$ pour tous les nœuds $v'$ de
	l'orbite d'instanciation $O$ sont des orbites supprimées dans la règle
	instanciée $r(\underline{m})$. Et donc, d'après le théorème~\ref{thm:preservation-coherence-topo-gcarte}, toutes les orbites
	$G\orbit{\omega}(m((v,v')))$ sont supprimées dans la transformation directe
	$G \xlongrightarrow{r,\underline{m}} H$, pour tous les nœuds $v'$ de l'orbite
	d'instanciation $O$.
	\\

	Réciproquement, supposons que toutes les orbites $G\orbit{\omega}(m((v,v')))$ sont
	supprimées dans la transformation directe $G \xlongrightarrow{r,\underline{m}} H$ pour tous
	les nœuds $v'$ de l'orbite d'instanciation $O$. Donc, d'après la proposition
	\ref{prop:base-rule-delete}, toutes les orbites $L(O)\orbit{\omega}((v,v'))$ pour
	tous les nœuds $v'$ de l'orbite d'instanciation $O$ sont des orbites
	supprimées dans la règle instanciée $r(\underline{m})$. Donc par construction
	de l'instanciation, $\underline{L}\orbit{\omega}(v)$ est une orbite supprimée dans
	la règle $r$.
\end{proof}

\maxime{Instanciation + description}

\subsubsection{Scission}

\begin{figure}[htbp!]
	\centering
	\begin{subfigure}{.45\textwidth}
		\centering
		\includegraphics[width=.6\textwidth]{g-carte-triangulation-scission-G}
		\caption{\label{fig:formalisation-evenements-regles-jerboa-triangulation-face-g}$G$}
	\end{subfigure}
	\begin{subfigure}{.45\textwidth}
		\centering
		\includegraphics[width=.6\textwidth]{g-carte-triangulation-scission-H}
		\caption{\label{fig:formalisation-evenement-regles-jerboa-triangulation-face-h}$H$}
	\end{subfigure}
	\caption{\label{fig:formalisation-evenement-regle-jerboa-triangulation-face}Triangulation
		dans une G-carte~: scission d'une face}
\end{figure}


Reprenons la règle de triangulation
(Figure~\ref{fig:formalisation-evenement-regles-jerboa-regle-triangulation}) et son instanciation (Figure~\ref{fig:formalisation-evenement-regle-jerboa-triangulation-face}). %
La règle filtre une face $L\orbit{0,1,3}(n0)$ puis construit sa triangulation
$R\orbit{0,1,3}(n0)$. %
On peut observer, dans la règle, que l'orbite $L\orbit{0,1,3}(n0)$ présente
toutes les étiquettes de $\orbit{0,1,3}$, on s'intéresse particulièrement au
$1$-étiquettes puisque la face filtrée est scindée au niveau de ses
sommets. %
Dans l'orbite $R\orbit{0,1,3}(n0)$, aucun des n\oe uds ne présente de
$1$-étiquette. %
Dans cette règle, cette réécriture d'étiquettes caractérise la scission d'une face.

\begin{figure}[htbp!]
	\centering
	\includegraphics[height=50pt]{rule-unsew-a3}
	\caption{Règle de décousure de deux faces liées par des $3$-liaisons}
	\label{fig:formalisation-evenement-regles-jerboa-regle-decousure-faces}
\end{figure}

\begin{figure}[htbp!]
	\centering
	\begin{subfigure}{.45\textwidth}
		\centering
		\includegraphics[width=.85\textwidth]{g-carte-decousure-3-G}
		\caption{\label{fig:formalisation-evenements-regles-jerboa-evenement-scission-explicite-g}$G$}
	\end{subfigure}
	\begin{subfigure}{.45\textwidth}
		\centering
		\includegraphics[width=.85\textwidth]{g-carte-decousure-3-H}
		\caption{\label{fig:formalisation-evenement-regles-jerboa-evenement-scission-explicite-face-h}$H$}
	\end{subfigure}
	\caption{\label{fig:formalisation-evenement-regle-evenement-scission-explicite}Triangulation
		dans une G-carte~: scission d'une face}
\end{figure}

Prenons un autre exemple de scission avec la règle de décousure de deux volumes
(Figure~\ref{fig:formalisation-evenement-regles-jerboa-regle-decousure-faces}). %
Ici, la face $L\orbit{0,1,3}(n0)$ est scindée en deux dans $R$ puisque l'arc les
reliant est découpé et forme une boucle auprès de chaque n\oe ud. %
Cette scission est dite \emph{explicite}, à l'inverse des scission par réécritures des
étiquettes qui sont dites \emph{implicites}

\begin{proposition}[Scission d'orbite]
	Soit une règle $r~: L \longrightarrow R$, un nœud d'accroche $h$ de $L$, un morphisme de
	filtrage $\underline{m}~: \underline{L} \rightarrow R$ sur une G-carte $G$, et un type d'orbite
	$\orbit{\omega}$.

	Soit un nœud préservé $v_1$ de $L$ et $R$, $\underline{L}\orbit{\omega}(v_1)$ est scindé dans la règle si :
	\begin{itemize}
		\item la scission est due à des arcs explicites, c'est-à-dire si $\underline{L}\orbit{\omega}(v_1)$ est scindée dans $\underline{R}$~;

		\item la scission est due à des arcs implicites, c'est-à-dire si il existe un nœud $v$ de $\underline{L}\orbit{\omega}(v_1)$ dont l'étiquette $l(v)$ a pour k-ème position une dimension de $\orbit{\omega}$, et que pour tous les nœuds $v'$ de $\underline{R}\orbit{\omega}(v_1)$ l'étiquette de $l(v')$ a pour k-ème position est un "$\_$" ou une dimension qui n'appartient pas à $\orbit{\omega}$.
	\end{itemize}

	Soit deux nœuds préservés $v_1$ et $v_2$ de $L$ et $R$. Notons $O = G\orbit{l(h)}(\underline{m}(h))$ l'orbite d'instanciation.
	\begin{enumerate}

		\item
		      Si $\underline{L}\orbit{\omega}(v_1)$ est scindée en deux orbites $\underline{R}\orbit{\omega}(v_1)$ et $\underline{R}\orbit{\omega}(v_2)$ dans la règle du à des arcs explicites.
		      Et si l'orbite $\underline{R}\orbit{\omega}(v_1)$ est entièrement filtrée, c'est-à-dire que pour tout nœud $v$ de $\underline{R}\orbit{\omega}(v_1)$ et pour toute dimension $i$ de $\orbit{\omega}$, $v$ est la source d'un $i$-arc implicite ou explicite dans~$R$.
		      Alors l'orbite $G\orbit{\omega}(m((v_1,v')))$ est scindée en deux orbites $H\orbit{\omega}(m'((v_1, v')))$ et $H\orbit{\omega}(m'((v_2, v')))$ dans la transformation directe $G \xlongrightarrow{r,\underline{m}} H$ pour tout nœud $v'$ de l'orbite d'instanciation $O$.

		\item Si l'orbite $\underline{L}\orbit{\omega}(v_1)$ est scindée en deux orbites $\underline{R}\orbit{\omega}(v_1)$ et
		      $\underline{R}\orbit{\omega}(v_2)$ dans la règle due à un arc explicite.
		      Si les orbites $\underline{R}\orbit{\omega}(v_1)$ et $\underline{R}\orbit{\omega}(v_2)$
		      sont toutes deux partiellement filtrées.
		      Alors, pour chaque nœud $v'$ de l'orbite d'instanciation $O$, soit $G\orbit{\omega}(m((v_1,v')))$ est scindée en deux orbites
		      $H\orbit{\omega}(m((v_1, v')))$ et $H\orbit{\omega}(m((v_2,v')))$, soit l'orbite est modifiée, dans la transformation directe
		      $G \xlongrightarrow{r, \underline{m}} H$.

		\item Si l'orbite $\underline{L}\orbit{\omega}(v_1)$ est scindée dans la règle du au $k$-ième arc implicite.
		      Alors pour tout nœud $v'_1$ de l'orbite d'instanciation $O$ et $v'_2$ le $i$-voisin de $v'_1$ dans $O$ avec $i$ la dimension à la $k$-ième position de $l(h)$, $G\orbit{\omega}(m((v_1,v'_1)))$ est soit scindée en deux orbites
		      $H\orbit{\omega}(m((v_1, v'_1)))$ et $H\orbit{\omega}(m((v_1,v'_2)))$, soit modifiée en une orbite unique $H\orbit{\omega}(m((v_1, v'_1))) = H\orbit{\omega}(m((v_1,v'_2)))$ dans la transformation directe
		      $G \xlongrightarrow{r, \underline{m}} H$.
	\end{enumerate}

\end{proposition}

\begin{proof}
	Soit deux nœuds préservés $v_1$ et $v_2$ de $L$ et $R$.
	\begin{enumerate}
		\item Supposons que $\underline{L}\orbit{\omega}(v_1)$ est scindée en deux
		      orbites $\underline{R}\orbit{\omega}(v_1)$ et $\underline{R}\orbit{\omega}(v_2)$ dans la
		      règle du à des arcs explicites. Alors, par définition, il existe un chemin
		      $e_1 e_2 ... e_j$ dans $L$ de source $v_1$, de cible $v_2$ et étiqueté par
		      $i_1 i_2 ... i_j$ sur $\orbit{\omega}$, mais il n'existe pas de tel chemin
		      $f_1 f_2 ... f_k$ dans $R$ de source $v_1$, de cible $v_2$ et étiqueté sur
		      $\orbit{\omega}$.

		      Donc dans la règle instanciée, pour tout nœud $v'$ de l'orbite
		      d'instanciation $O = G\orbit{l(h)}(\underline{m}(h))$, il existe un chemin
		      $(e_1,v') (e_2,v') ... (e_j,v')$ dans le motif gauche de la règle instanciée
		      $L(O)$ dont la source est $(s(e_1),v') = (v_1, v')$, la cible est
		      $(t(e_j), v') = (v_2, v')$ et étiqueté par le même mot $i_1 i_2 ... i_j$ sur
		      $\orbit{\omega}$.

		      Supposons que pour un nœud $v'$ de l'orbite d'instanciation $O$, il
		      existe un chemin $(f_1,v') (f_2,v') ... (f_k,v')$ dans le motif droit de la
		      règle instanciée $R(O)$ dont la source est $(s(f_1),v') = (v_1, v')$, la cible
		      est $(t(e_j), v') = (v_2, v')$ et étiqueté sur $\orbit{\omega}$. Alors, par
		      construction de l'instanciation, le chemin $f_1 f_2 ... f_k$ dans $R$ de source
		      $v_1$, de cible $v_2$ et étiqueté sur $\orbit{\omega}$ existe dans la règle de
		      départ. Ce qui est contradictoire avec l'hypothèse. Donc il n'existe pas de
		      chemin $(f_1,v') (f_2,v') ... (f_k,v')$ dans le motif droit de la règle
		      instanciée ayant les propriétés attendues. Donc $R(O)\orbit{\omega}((v_1,v'))$ et
		      $R(O)\orbit{\omega}((v_2,v'))$ sont deux $\orbit{\omega}$-orbites distinctes.

		      Donc d'après la proposition \ref{prop:base-rule_split}, si
		      $\underline{R}\orbit{\omega}(v_1)$ et donc $R(O)\orbit{\omega}((v_1, v'))$ est entièrement
		      filtrée, alors l'orbite $G\orbit{\omega}(m((v_1,v')))$ est scindée en deux orbites
		      $H\orbit{\omega}(m'((v_1, v')))$ et $H\orbit{\omega}(m'((v_2, v')))$ dans la
		      transformation directe $G \xlongrightarrow{r,\underline{m}} H$ pour tout nœud
		      $v'$ de l'orbite d'instanciation $O$. %

		\item
		      Supposons que $\underline{L}\orbit{\omega}(v_1)$ est scindée en deux orbites
		      $\underline{R}\orbit{\omega}(v_1)$ et $\underline{R}\orbit{\omega}(v_2)$ dans la règle du
		      à des arcs explicites.

		      Pour les mêmes raisons que dans le cas 1, $R(O)\orbit{\omega}((v_1,v'))$ et
		      $R(O)\orbit{\omega}((v_2,v'))$ sont deux $\orbit{\omega}$-orbites distinctes dans la
		      règles instanciée, pour tout nœud $v'$ de l'orbite d'instanciation $O$.

		      Donc d'après la proposition \ref{prop:base-rule_split}, si $R\orbit{\omega}(v_1)$
		      et donc $R(O)\orbit{\omega}((v_1, v'))$ est partiellement filtrée, alors l'orbite
		      $G\orbit{\omega}(m((v_1,v')))$ est soit scindée en deux orbites
		      $H\orbit{\omega}(m'((v_1, v')))$ et $H\orbit{\omega}(m'((v_2, v')))$, soit modifiée, dans
		      la transformation directe $G \xlongrightarrow{r,\underline{m}} H$ pour tout nœud
		      $v'$ de l'orbite d'instanciation $O$. %

		\item
		      Supposons que $\underline{L}\orbit{\omega}(v_1)$ est scindée due au $k$-ième arc
		      implicite. Soit $v'_1$ un nœud de l'orbite d'instanciation
		      $O = G\orbit{l(h)}(\underline{m}(h))$ et $v'_2$ le $i$-voisin de $v'_1$ dans
		      $O$, avec $i$ la $k$-ième dimension de l'étiquette $l(h)$ du nœud d'accroche $h$
		      de $L$.

		      Par hypothèse, l'un des nœuds $v$ de $\underline{L}\orbit{\omega}(v_1)$ a une
		      $k$-ième dimension $j$ dans l'orbite $\orbit{\omega}$ ($j \in \orbit{\omega}$), alors que tous
		      des n\oe uds de $\underline{R}\orbit{\omega}(v_1)$ ont leur $k$-ième dimension hors de
		      $\orbit{\omega}$. Donc les nœuds $(v, v'_1)$ et $(v, v'_2)$ sont $j$-voisins dans
		      l'instanciation $L(O)$, donc $(v, v'_1)$ et $(v, v'_2)$ sont dans la même
		      $\orbit{\omega}$-orbite dans $L(O)$, et donc $(v_1, v'_1)$ et $(v_2, v'_2)$ aussi.

		      Nous avons plusieurs cas :
		      \begin{itemize}
			      \item si $(v_1, v'_1)$ et $(v_2, v'_2)$ sont dans deux $\orbit{\omega}$-orbites
			            différentes de $R(O)$, d'après la proposition \ref{prop:base-rule_split}, selon
			            les cas, $H\orbit{\omega}(m'((v_1, v'_1)))$ et $H\orbit{\omega}(m'((v_1,v'_2)))$ sont dans
			            deux orbites différentes ou non de $H$, avec $H$ le résultat de la
			            transformation directe $G \xlongrightarrow{r, \underline{m}} H$ ; autrement dit
			            $G\orbit{\omega}(m((v_1, v'_1))$ est scindée en $H\orbit{\omega}(m'((v_1, v'_1)))$ et
			            $H\orbit{\omega}(m'((v_1,v'_2)))$ ou juste modifiée par la transformation directe
			            $G \xlongrightarrow{r, \underline{m}} H$ ;

			      \item si au contraire $(v_1, v'_1)$ et $(v_2, v'_2)$ sont dans la même
			            $\orbit{\omega}$-orbites de $R(O)$ (car il existe un chemin $e_1 e_2 ... e_l$ de $O$
			            de source $v'_1$ et de cible $v'_2$ qui ne comprend pas l'étiquette $j$, ce
			            chemin est réécrit en un chemin $(w, e_1) (w,e_2) ... (w, e_l)$ de $R(O)$ dont
			            toutes les étiquettes sont dans $\orbit{\omega}$), d'après la proposition
			            \ref{prop:base-rule-modify}, alors $G\orbit{\omega}(m((v_1, v'_1))$ est modifiée par
			            la transformation directe $G \xlongrightarrow{r, \underline{m}} H$. %
		      \end{itemize}
	\end{enumerate}
\end{proof}

\begin{definition}[Orbite d'origine]
	Soit une règle $r~: L \longrightarrow R$, $v_1$ et $v_2$ deux nœuds préservés de $L$ et $R$ et $\orbit{\omega}$ un type d'orbite, tels que l'orbite $L\orbit{\omega}(v_1) = L\orbit{\omega}(v_2)$ de $L$ soit scindée en deux orbites distinctes $R\orbit{\omega}(v_1)$ et $R\orbit{\omega}(v_2)$ dans $R$.

	Une \emph{orbite d'origine} de $R\orbit{\omega}(v_1)$ dans $L$ (et de manière symétrique l'orbite d'origine de $R\orbit{\omega}(v_2)$ dans $L$) est une orbite $L\orbit{\omega'}(w)$ où :
	\begin{itemize}
		\item où $w$ est un nœud de $\underline{L}\orbit{\omega}(v_1)$,

		\item $\orbit{\omega'}$ est l'ensemble des dimensions  $i$, où $i$ est renommé dans un arc implicite de $w$ ; c'est-à-dire que $(l_L(w) \mapsto l_R(v_1))(i) = j \in \orbit{\omega}$

	\end{itemize}
	Soit un morphisme de filtrage $\underline{m} : \underline{L} \rightarrow G$ sur une G-carte $G$, $G \xlongrightarrow{r, \underline{m}} H$ la transformation directe correspondante et $O$ l'orbite d'instanciation. Soit $v'$ un nœud de $O$.

	Une {\em origine} de l'orbite $H\orbit{\omega}((v_1,v'))$ est une orbite $G\orbit{\omega'}((w,v'))$.
\end{definition}

\todo{Ajouter illustration règle insertion d'arête coloriée (dans le document il s'agit de la règle dépliée, je mettrai la règle repliée)}
% \begin{figure}[H]
%     \centering
%     \includegraphics[width=\linewidth]{figures/colored-rule-insert-edge.pdf}
%     \caption{Règle $L \longrightarrow R$ d'insertion d'arête}
%     \label{fig:colored-rule-insert-edge}
% \end{figure}

\todo{Ajouter illustration cube insertion arête}
% \begin{figure}[H]
%     \begin{subfigure}{0.4\linewidth}
%         \centering
%         \includegraphics[width=\linewidth]{figures/inst-cube-insert-edge-left-filter.pdf}
%         \caption{}
%         \label{fig:inst-cube-insert-edge-orbits}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}{0.4\linewidth}
%         \centering
%         \includegraphics[width=\linewidth]{figures/inst-cube-insert-edge-final.pdf}
%         \caption{}
%         \label{fig:inst-cube-insert-edge-final}
%     \end{subfigure}
%     \caption{Instanciation de la règle d'insertion d'arête entre deux sommets non-voisins appartenant à la même face}
% \end{figure}

\todo{Ajouter illustration «~cylindre~» insertion arête}
% \begin{figure}[H]
%     \begin{subfigure}{0.3\linewidth}
%         \centering
%         \includegraphics[width=\linewidth]{figures/inst-cylinder-insert-edge-left-filter.pdf}
%         \caption{}
%         \label{fig:inst-cylinder-insert-edge-orbits}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}{0.3\linewidth}
%         \centering
%         \includegraphics[width=\linewidth]{figures/inst-cylinder-insert-edge-final.pdf}
%         \caption{}
%         \label{fig:inst-cylinder-insert-edge-final}
%     \end{subfigure}
%     \caption{Instanciation de la règle d'insertion d'arête entre deux sommets de deux faces distinctes}
% \end{figure}

\maxime{Instanciation + description}


\subsubsection{Fusion}

\todo{Ajouter illustration règle contraction de face}
% \begin{figure}[H]
%     \centering
%     \includegraphics[scale=.6]{figures/colored-rule-face-collapse.pdf}
%     \caption{Règle de fusion de sommets par contraction d'une face}
%     \label{fig:colored-rule-face-collapse}
% \end{figure}

\todo{Ajouter illustrations cube contraction de face}
% \begin{figure}[H]
%     \centering
%     \begin{subfigure}{0.45\linewidth}
%         \centering
%         \includegraphics[width=.75\linewidth]{figures/inst-cube-face-collapse-left-filter.pdf}
%         \caption{}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}{.45\linewidth}
%         \centering
%         \includegraphics[width=.85\linewidth]{figures/inst-cube-face-collapse-final-2.pdf}
%         \caption{}
%     \end{subfigure}
% \end{figure}

La figure \textbf{fig:colored-rule-face-collapse} représente la règle de contraction d'une face. Outre la suppression de la face (orbite $\orbit{0, 1, 3}$) filtrée par les nœuds $n_0$ et $n_1$, elle fusionne tous les sommets (orbites $\orbit{1, 2, 3}$) filtrés par l'ensemble des nœuds de la règle.
%, fusionnant les sommets incidents à cette dernière, avec l'orbite $n_0$ dans $L$ qui filtre la face et les orbites $n_0$, $n_1$ et $n_2$ dans $R$ qui filtrent les nœuds pour la fusion
\maxime{instanciation et description nécessaire + règle de fusion "explicite"}.


\begin{proposition}[Fusion d'orbites]
	Soit une règle $r~: L \longrightarrow R$, un nœud d'accroche $h$ de $L$, un morphisme de filtrage $\underline{m}~: \underline{L} \rightarrow R$ sur une G-carte $G$, et un type d'orbite $\orbit{\omega}$.

	Soit deux nœuds préservés $v_1$ et $v_2$ de $L$ et $R$, $\underline{L}\orbit{\omega}(v_1)$ et $\underline{L}\orbit{\omega}(v_2)$ sont fusionnées dans la règle~:

	\begin{itemize}
		\item si la fusion est due à des arcs explicites, c'est-à-dire si $\underline{L}\orbit{\omega}(v_1)$ et $\underline{L}\orbit{\omega}(v_2)$ sont fusionnées dans $\underline{R}$~;

		\item si la fusion est due à des arcs implicites, c'est-à-dire si pour tout nœud de $\underline{L}\orbit{\omega}(v_1)$, l'étiquette $l(v)$ a pour $k$-ème position un "\_" ou une
		      dimension qui n'appartient pas à $\orbit{\omega}$, et qu'il existe un nœud $v'$ de $\underline{R}\orbit{\omega}(v_1)$ dont l'étiquette $l(v')$ a pour $k$-ème position une dimension
		      de $\orbit{\omega}$.
	\end{itemize}

	Soit deux nœuds préservés $v_1$ et $v_2$ de $L$ et $R$. Notons $O = G\orbit{l(h)}(\underline{m}(h))$ l'orbite d'instanciation.

	\begin{enumerate}
		\item Si $\underline{L}\orbit{\omega}(v_1)$ et $\underline{L}\orbit{\omega}(v_2)$ sont fusionnées en une orbite $\underline{R}\orbit{\omega}(v_1)$ dans la règle du à des arcs explicites.
		      Et si l'orbite $\underline{L}\orbit{\omega}(v_1)$ est entièrement filtrée, c'est-à-dire que pour tout nœud $v$ de $\underline{L}\orbit{\omega}(v_1)$ et pour toute dimension $i$ de $\orbit{\omega}$, $v$ est la source d'un $i$-arc implicite
		      ou explicite dans $L$. Alors les orbites $G\orbit{\omega}(m((v_1,v')))$ et $G\orbit{\omega}(m((v_2,v')))$ sont fusionnées en une orbite $H\orbit{\omega}(m'((v_1,v')))$ dans la transformation directe
		      $G \xlongrightarrow{r,\underline{m}} H$ pour tout nœud $v'$ de l'orbite d'instanciation $O$.

		\item Si les orbites $\underline{L}\orbit{\omega}(v_1)$ et $\underline{L}\orbit{\omega}(v_2)$ sont fusionnées en une orbite $\underline{R}\orbit{\omega}(v_1)$ dans la règle du à des arcs explicites.
		      Si les orbites $\underline{L}\orbit{\omega}(v_1)$ et $\underline{L}\orbit{\omega}(v_2)$ sont toutes deux partiellement filtrées. Alors, pour chaque nœud $v'$ de l'orbite
		      d'instanciation $O$, soit $G\orbit{\omega}(m((v_1,v')))$ et $G\orbit{\omega}(m((v_2,v')))$ sont fusionnées en une orbite
		      $H\orbit{\omega}(m'((v_1,v')))$, soit elles sont deux parties d'une même orbite modifiée dans la transformation directe $G \xlongrightarrow{r, \underline{m}} H$.

		\item Si l'orbite $\underline{L}\orbit{\omega}(v_1)$ est fusionnées dans la règle du au $k$-ème arc implicite.
		      Alors pour tout nœud $v_1'$ de l'orbite d'instanciation $O$ et $v_2'$ le $i$-voisin de $v_1'$ dans $O$ avec $i$ la dimension à la $k$-ème position
		      de $l(h)$, $G\orbit{\omega}(m((v_1,v_1')))$ et $G\orbit{\omega}(m((v_1,v_2')))$ sont soit fusionnées en une orbite $H\orbit{\omega}(m'((v_1,v_1')))$, soit modifiées en une orbite unique
		      $H\orbit{\omega}(m'((v_1,v_1'))) = H\orbit{\omega}(m'((v_1,v_2')))$ dans la transformation directe $G \xlongrightarrow{r,\underline{m}} H$
	\end{enumerate}
\end{proposition}

\begin{proof}
	Soit deux nœuds préservés $v_1$ et $v_2$ de $L$ et $R$
	\begin{enumerate}
		\item Supposons que $\underline{L}\orbit{\omega}(v_1)$ et
		      $\underline{L}\orbit{\omega}(v_2)$ sont fusionnées en une orbite
		      $\underline{R}\orbit{\omega}(v_1)$ dans la règle du à des arcs
		      explicites. Alors, par définition, il n'existe pas de chemin
		      $e_1 e_2 \dots e_j$ dans $L$ de source $v_1$, de cible $v_2$ et
		      étiqueté par $i_1 i_2 \dots i_j$ sur $\orbit{\omega}$, mais il existe un
		      chemin $f_1 f_2 \dots f_k$ dans $R$ de source $v_1$, de cible $v_2$
		      et étiqueté sur $\orbit{\omega}$.

		      Donc dans la règle instanciée, pour tout nœud $v'$ de l'orbite d'instanciation $O = G\orbit{l(h)}(\underline{m}(h))$, il n'existe pas de chemin $(e_1, v')(e_2, v')\dots(e_j,v')$ dans le
		      motif gauche de la règle instanciée $L(O)$ dont la source est $(s(e_1),v') = (v_1,v')$, la cible est $(t(e_j),v') = (v_2,v')$ et étiqueté par le même mot $i_1 i_2 \dots i_j$ sur $\orbit{\omega}$.

		      Supposons que pour tout nœud $v'$ de l'orbite d'instanciation $O$, il n'existe pas de chemin $(f_1,v')(f_2,v')\dots(f_k,v')$ dans le motif droit de la règle instanciée $R(O)$ dont la source
		      est $(s(f_1),v') = (v_1,v')$, la cible est $(t(f_k),v') = (v_2, v')$ et étiqueté sur $\orbit{\omega}$.
		      Alors, par construction de l'instanciation, le chemin $f_1 f_2 \dots f_k$ dans $R$ de source $v_1$,
		      de cible $v_2$ et étiqueté sur $\orbit{\omega}$ n'existe pas dans la règle de départ.
		      Ce qui est contradictoire avec l'hypothèse. Donc il existe un chemin $(f_1,v')(f_2,v')\dots(f_k,v')$ dans le
		      motif droit de la règle instanciée ayant les propriétés attendues.
		      Donc $R(O)\orbit{\omega}((v_1,v'))$ et $R(O)\orbit{\omega}((v_2,v'))$ sont bien une seule et même $\orbit{\omega}$-orbite.

		      Donc d'après la proposition \ref{prop:base-rule-merge}, si $\underline{L}\orbit{\omega}(v_1)$ et donc $L(O)\orbit{\omega}((v_1,v'))$ est entièrement filtrée, alors les orbites $G\orbit{\omega}(m((v_1,v')))$ et
		      $G\orbit{\omega}(m((v_2,v')))$ sont fusionnées en une orbite $H\orbit{\omega}(m'((v_1,v')))$ dans la transformation directe $G \xlongrightarrow{r,\underline{m}} H$ pour tout nœud $v'$ de l'orbite
		      d'instanciation $O$.

		\item Supposons que $\underline{L}\orbit{\omega}(v_1)$ et
		      $\underline{L}\orbit{\omega}(v_2)$ sont fusionnées en une orbite
		      $\underline{R}\orbit{\omega}(v_1)$ dans la règle du à des arcs
		      explicites.

		      Pour les mêmes raisons que dans le cas $1$, $L(O)\orbit{\omega}((v_1,v'))$ et $L(O)\orbit{\omega}((v_2,v'))$ sont deux $\orbit{\omega}$-orbites distinctes dans la règle instanciée, pour tout nœud $v'$ de l'orbite d'instanciation $O$.

		      Donc d'après la proposition \ref{prop:base-rule-merge}, si $\underline{L}\orbit{\omega}(v_1)$ et donc $L(O)\orbit{\omega}((v_1,v'))$ est partiellement filtrée, alors les orbites
		      $G\orbit{\omega}(m((v_1,v')))$ et $G\orbit{\omega}(m((v_2,v')))$ sont soit fusionnées en une orbite $H\orbit{\omega}(m'((v_1,v')))$, soit modifiées, dans la transformation directe
		      $G \xlongrightarrow{r,\underline{m}} H$ pour tout nœud $v'$ de l'orbite d'instanciation $O$.

		\item Supposons que $\underline{L}\orbit{\omega}(v_1)$ est fusionnée due au
		      $k$-ème arc implicite. Soit $v_1'$ un nœud de l'orbite
		      d'instanciation $O = G\orbit{l(h)}(\underline{m}(h))$ et
		      $v_2'$ le $i$-voisin de $v_1'$ dans $O$, avec $i$ la $k$-ème
		      dimension de l'étiquette $l(h)$ du nœud d'accroche $h$ de $L$.

		      Par hypothèse, tous les n\oe uds de $\underline{L}\orbit{\omega}(v_1)$ ont une $k$-ième dimension soit supprimée, soit renommée hors de $\orbit{\omega}$.
		      Au contraire, il existe un nœud $v$ de $\underline{R}\orbit{\omega}(v_1)$ dont la $k$-ième dimension est renommée en $j$ dans $\orbit{\omega}$. Donc les nœuds $(v, v'_1)$ et $(v, v'_2)$ sont $j$-voisins dans l'instanciation $R(O)$, donc $(v, v'_1)$ et $(v, v'_2)$ sont dans la même $\orbit{\omega}$-orbite dans $R(O)$, et donc $(v_1, v'_1)$ et $(v_2, v'_2)$ aussi.

		      Nous avons plusieurs cas :
		      \begin{itemize}
			      \item si $(v_1, v'_1)$ et $(v_2, v'_2)$ sont deux $\orbit{\omega}$-orbites différentes de $L(O)$, d'après la proposition \ref{prop:base-rule_split},
			            selon les cas, $G\orbit{\omega}(m((v_1, v'_1)))$ et $G\orbit{\omega}(m((v_1,v'_2)))$ sont dans deux orbites différentes ou non de $G$ ;
			            autrement dit $G\orbit{\omega}(m((v_1, v'_1))$ et $G\orbit{\omega}(m((v_2, v'_2))$ sont fusionnées en une orbite $H\orbit{\omega}(m'((v_1, v'_1)))$ ou l'unique orbite $G\orbit{\omega}(m((v_1, v'_1)) = G\orbit{\omega}(m((v_2, v'_2))$ est juste modifiée par la transformation directe $G \xlongrightarrow{r, \underline{m}} H$ ;

			      \item si au contraire $(v_1, v'_1)$ et $(v_2, v'_2)$ sont dans la même $\orbit{\omega}$-orbites de $L(O)$ (car il existe un chemin $e_1 e_2 ... e_l$ de $O$ de source $v'_1$ et de cible $v'_2$ qui ne comprend pas l'étiquette $j$, ce chemin est réécrit un chemin $(w, e_1) (w,e_2) ... (w, e_l)$ de $L(O)$ dont toutes les étiquettes sont dans $\orbit{\omega}$),
			            d'après la proposition \ref{prop:base-rule-modify}, alors $G\orbit{\omega}(m((v_1, v'_1))$ est modifiée par la transformation directe $G \xlongrightarrow{r, \underline{m}} H$.
		      \end{itemize}
	\end{enumerate}
\end{proof}

\todo{Ajouter illustration règle suppression arête}
% \begin{figure}[H]
%     \centering
%     \includegraphics{figures/colored-rule-delete-edge.pdf}
%     \caption{Règle de suppression d'arête}
%     \label{fig:colored-rule-delete-edge}
% \end{figure}

\todo{Ajouter illustrations cube suppression arête}
% \begin{figure}[H]
%     \centering
%     \begin{subfigure}{.3\linewidth}
%         \centering
%         \includegraphics[width=\linewidth]{figures/inst-cube-delete-edge-left-filter.pdf}
%         \caption{}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}{.45\linewidth}
%         \centering
%         \includegraphics[width=.68\linewidth]{figures/inst-cube-delete-edge-final.pdf}
%         \caption{}
%     \end{subfigure}
%     \hfill
% \end{figure}

\todo{Ajouter illustrations «~cylindre~» suppression arête}
% \begin{figure}[H]
%     \centering
%     \begin{subfigure}{.35\linewidth}
%         \centering
%         \includegraphics[width=\linewidth]{figures/inst-cylinder-delete-edge-left-filter.pdf}
%         \caption{}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}{.45\linewidth}
%         \centering
%         \includegraphics[width=.68\linewidth]{figures/inst-cylinder-delete-edge-final.pdf}
%         \caption{}
%     \end{subfigure}
%     \hfill
% \end{figure}

\maxime{Instanciation + description}


\subsubsection{Non-changement}

% Dans l'insertion de sommet figure \textbf{fig:rule-insert-vertex}, le nœud $n0$
% (en noir) filtre partiellement les orbites sommet aux extrémités de l'arête sur
% laquelle est inséré le nouveau sommet. Ces orbites sont inchangées par
% l'insertion du nouveau sommet.

La règle de triangulation
(Figure~\ref{fig:formalisation-evenement-regles-jerboa-regle-triangulation}),
réécrit les étiquettes du n\oe ud $n0$, qui étaient $\orbit{0,\_,3}$ dans $L$ et
deviennent $\orbit{0,1,3}$ dans $R$, l'étiquette $0$ est préservée. %
Le n\oe ud $n0$ filtre ainsi des arêtes qui restent inchangées suite à la
réécriture des étiquettes du n\oe ud. %

\begin{proposition}[Non-changement d'orbite]
	Soit une règle $r~: L \longrightarrow R$, un nœud d'accroche $h$ de $L$,
	un morphisme de filtrage $\underline{m}~: \underline{L} \rightarrow G$ sur une G-carte
	$G$, et un type d'orbite $\orbit{\omega}$.

	Soit $v$ un nœud de $L$. L'orbite $\underline{L}\orbit{\omega}(v)$ est inchangée dans
	la règle si et seulement si toutes les orbites $G\orbit{\omega}(m((v,v')))$ sont
	inchangées dans la transformation directe $G \xlongrightarrow{r,\underline{m}} H$, pour tous
	les nœuds $v'$ de l'orbite d'instanciation $G\orbit{l(h)}(\underline{m}(h))$.
\end{proposition}

\begin{proof} Soit $v$ un nœud préservé de $L \cap R$ tel que l'orbite
	$\underline{L}\orbit{\omega}(v)$ est inchangée dans la règle $r~: L \longrightarrow R$. Soit
	$O = G\orbit{l(h)}(\underline{m}(h))$, l'orbite d'instanciation.

	Soit $w$ un nœud quelconque de l'orbite $\underline{L}\orbit{\omega}(v)$. $w$ est un
	nœud inchangé de la règle de $L \cap R$. Donc, par construction de la règle
	instanciée, tous les nœuds $(w,v')$ pour tous les nœuds $v'$ de l'orbite
	d'instanciation $O$, sont des nœuds inchangés dans la règle instanciée
	$r(\underline{m})$. Or une orbite $G\orbit{\omega}(m((v,v')))$ n'est pas
	nécessairement entièrement filtrée, c'est-à-dire
	$L(O)\orbit{\omega}((v,v')) \subset G\orbit{\omega}(m((v,v')))$ et
	$R(O)\orbit{\omega}((v,v')) \subset H\orbit{\omega}(m'((v,v')))$. Par définition, la partie non
	filtrée est aussi inchangée
	$G\orbit{\omega}(m((v,v'))) \setminus L(O)\orbit{\omega}((v,v')) = H\orbit{\omega}(m'((v,v'))) \setminus R(O)\orbit{\omega}((v,v'))$.
	Donc toutes les orbites $L(O)\orbit{\omega}((v,v'))$ pour tous les nœuds $v'$ de
	l'orbite d'instanciation $O$ sont des orbites inchangées dans la règle
	instanciée $r(\underline{m})$. Et donc, d'après la proposition
	\ref{prop:base-rule-unchange}, toutes les orbites $G\orbit{\omega}(m((v,v')))$ sont
	inchangées dans la transformation directe $G \xlongrightarrow{r,\underline{m}} H$, pour tous
	les nœuds $v'$ de l'orbite d'instanciation $O$. \\

	Réciproquement, supposons que toutes les orbites $G\orbit{\omega}(m((v,v')))$ sont
	inchangées dans la transformation directe $G \xlongrightarrow{r, \underline{m}} H$, pour tous
	les nœuds $v'$ de l'orbite d'instanciation $O$. Donc d'après la proposition
	\ref{prop:base-rule-unchange}, toutes les orbites $L(O)\orbit{\omega}((v,v'))$ pour
	tous les nœuds $v'$ de l'orbite d'instanciation $O$ sont des orbites inchangées
	dans la règle instanciée $r(\underline{m})$. Donc par construction de
	l'instanciation, $\underline{L}\orbit{\omega}(v)$ est une orbite inchangée dans la
	règle $r$.
\end{proof}

\maxime{Instanciation + description}


\subsubsection{Sans effet}

\maxime{appliquer la règle d'insertion de sommet sur un carré}

Dans la figure ? le brin $a$ de $G$ n'est pas filtré, mais l'orbite sommet $G\orbit{1,2,3}(a)$ est partiellement filtrée par la règle \maxime{à continuer en fonction du dessin} […] donc l'orbite sommet $G\orbit{1,2,3}(a)$ est inchangée.
Le brin $b$ de $G$ n'est pas filtré et l'orbite $G\orbit{1,2,3}(b)$ n'est pas filtrée non plus, donc l'orbite sommet $G\orbit{1,2,3}(b)$ n'est pas affectée par l'application de la règle.

\begin{definition}[Orbite non affectée]
	Soit une règle $r~: L \longrightarrow R$, un nœud d'accroche $h$ de $L$, un morphisme de filtrage
	$\underline{m}~: \underline{L} \rightarrow G$ sur une G-carte $G$, et un type d'orbite
	$\orbit{\omega}$.

	Soit $b$ un brin de $G$ non filtré. L'orbite $G\orbit{\omega}(b)$ n'est \emph{pas affectée} par l'application de $r$ si $G\orbit{\omega}(b) \cap \underline{m}(L) = \emptyset$ et \emph{inchangée} sinon.

\end{definition}

\subsubsection{Modification}

\begin{proposition}[Modification d'orbite]
	Soit une règle $r~: L \longrightarrow R$, un nœud d'accroche $h$ de $L$, un morphisme de filtrage $\underline{m}~: \underline{L} \rightarrow G$ sur une G-carte $G$, et un type d'orbite $\orbit{\omega}$.
	Soit $v$ un nœud conservé de $L$ et $R$.

	L'orbite $\underline{L}\orbit{\omega}(v)$ est modifiée dans la règle si et seulement si on à la fois :
	\begin{itemize}
		\item la $\orbit{\omega}$-orbite incidente à $v$ est modifiée entre $\underline{L}$ et $\underline{R}$,
		\item pour toute position $k$ des étiquettes de nœud de la règle, il existe un nœud $v_1$ de $\underline{L}\orbit{\omega}(v)$ dont la $k$-ième dimension appartient à $\orbit{\omega}$ si et seulement si il existe un nœud $v_2$ de $\underline{R}\orbit{\omega}(v)$ dont la $k$-ième dimension appartient à~$\orbit{\omega}$.
	\end{itemize}


	Si l'orbite $\underline{L}\orbit{\omega}(v)$ est modifiée dans la règle $r$, alors
	$G\orbit{\omega}(m((v,v')))$ est modifiée dans la transformation directe
	$G \xlongrightarrow{r,\underline{m}} H$ pour tous les nœuds $v'$ de l'orbite d'instanciation
	$G\orbit{l(h)}(\underline{m}(h))$. Réciproquement, si une orbite
	$G\orbit{\omega}(m((v,v')))$ est modifiée dans la transformation directe
	$G \xlongrightarrow{r,\underline{m}} H$, alors $\underline{L}\orbit{\omega}(v)$ est modifiée,
	scindée ou fusionnée dans la règle.
\end{proposition}

\begin{proof}
	Soit $v$ un nœud conservé de $L$ et $R$. Soit $O = G\orbit{l(h)}(\underline{m}(h))$, l'orbite d'instanciation.

	~

	Supposons que l'orbite $\underline{L}\orbit{\omega}(v)$ est modifiée dans la règle.
	Soit $w$ un nœud de l'orbite $\underline{L}\orbit{\omega}(v)$ conservé par la règle.
	Par définition, $w$ est un nœud de $\underline{R}\orbit{\omega}(v)$.

	Notons $e_1...e_i$ un chemin de $\underline{L}\orbit{\omega}(v)$ de source $v$ et de cible $w$, $f_1...f_j$ un chemin de $\underline{R}\orbit{\omega}(v)$ de source $v$ et de cible $w$, et $e'_1...e'_l$ un chemin de $O$. Notons $v'_{m_1}$ la source de $e'_m$ et $v'_m$ sa cible.

	\agnes{en cours}
\end{proof}


\subsection{Origine des orbites lors des applications des règles}

\maxime{Choisir entre ici et scission pour la définition d'origine}
\begin{definition}[Orbite d'origine]
	Soit une règle $r~: L \longrightarrow R$, $n$ un nœud de $R$ et $\orbit{\omega}$ un type d'orbite.

	Une \emph{orbite d'origine} de $\underline{R}\orbit{\omega}(n)$ dans $L$ est une orbite $\underline{L}\orbit{\omega'}(n')$ où :
	\begin{itemize}
		\item où $n'$ est un nœud de $L$,

		\item $\orbit{\omega'}$ est l'ensemble des dimensions  $i$, où $i$ est renommé dans un arc implicite de $n'$ ; c'est-à-dire que $(l_L(n') \mapsto l_R(n))(i) = j \in \orbit{\omega}$

	\end{itemize}
	Soit un morphisme de filtrage $\underline{m} : \underline{L} \rightarrow G$ sur une G-carte $G$, $G \xlongrightarrow{r, \underline{m}} H$ la transformation directe correspondante et $O$ l'orbite d'instanciation. Soit $v'$ un nœud de $O$.

	Une {\em origine} de l'orbite $H\orbit{\omega}((n,v'))$ est une orbite $G\orbit{\omega'}((n',v'))$.
\end{definition}

Remarquons que lorsqu'une règle n'a pas de membre gauche ($L = \emptyset$), alors toutes les orbites de $R$ sont créées et aucune n'a d'origine.

\agnes{Donner les origines pour les principales orbites de la règle précédente (celles qui ne sont pas des suivis) + montrer ce que cela donne sur l'exemple}

Quand le type d'orbite $\orbit{\omega'}$ de l'origine est confondu avec l'orbite
d'intérêt ($\orbit{\omega'} = \orbit{\omega}$), alors l'orbite d'origine est identique à
celle de suivi (voir la section \ref{sec:formalisation-evenements-regles-jerboa-suivi}).

\agnes{Compléter l'exemple précédent pour les origines qui sont aussi des suivis + faire le lien avec la section précédente et les évènements correspondants}

\begin{table*}[htbp]
	\centering

	\caption{\label{tab:formalisation-evenement-regle-jerboa-tableau-detection}
		Extrait des évènement détectés lors de l'application de la règle de triangulation
		(Figure~\ref{fig:formalisation-evenement-regles-jerboa-regle-triangulation})
		sur la face d'un cube
		(Figure~\ref{fig:formalisation-evenement-regle-jerboa-triangulation-face})}

	\begin{tabular}{|p{0.1\textwidth}|p{0.35\textwidth}||p{0.1\textwidth}|p{0.35\textwidth}|}
		\hline
		\centering Type d'orbite & Évènement détecté                                                                                                                       & \centering Type d'orbite & Évènement détecté \\
		\hline\hline
		\centering $\orbit{0}$
		                         & Création $H\orbit{0}(a_{1})$
		\par Création~$H\orbit{0}(b_{1})$ \ldots
		\vspace{0.1 cm}
		\par Non-changement~$H\orbit{0}(a_{0})$
		\par Non-changement~$H\orbit{0}(c_{0})$~\ldots
		                         & \centering $\orbit{1}$
		                         & Création $H\orbit{0}(a_{2})$
		\par Création~$H\orbit{1}(c_{2})$ \ldots
		\vspace{0.1 cm}
		\par Scission~$G\orbit{1}(a_{0}) \rightarrow \{$$H\orbit{1}(a_{0})$, $H\orbit{1}(h_{0})\}$
		\par Scission~$G\orbit{1}(c_{0}) \rightarrow \{$$H\orbit{1}(c_{0})$, $H\orbit{1}(b_{0})\}$~\ldots                                                                                                                 \\
				\hline
				\centering $\orbit{2}$
		                         & Création $H\orbit{2}(a_{1})$
				\par Création~$H\orbit{2}(a_{2})$
				\par Création~$H\orbit{2}(b_{1})$
				\par Création~$H\orbit{2}(b_{2})$ \ldots
				\vspace{0.1 cm}
				\par Non-changement~$H\orbit{2}(a_{0})$
				\par Non-changement~$H\orbit{2}(b_{0})$
				\par Non-changement~$H\orbit{2}(c_{0})$
				\par Non-changement~$H\orbit{2}(d_{0})$~\ldots
		                         & \centering $\orbit{0,2}$
		                         & Création $H\orbit{0,2}(a_{1})$
				\par Création~$H\orbit{0,2}(b_{1})$\ldots
				\vspace{0.1 cm}
				\par Non-changement~$H\orbit{0,2}(a_{0})$
		\par Non-changement~$H\orbit{0,2}(c_{0})$~\ldots                                                                                                                                                                  \\
				\hline
				\centering $\orbit{0,1,2}$
		                         & Modification~$H\orbit{0,1,2}(a_{0})$
		                         & \centering $\orbit{0,1}$
		                         & Scission~$G\orbit{0,1}(a_{0}) \rightarrow \{H\orbit{0,1}(a_{0})$, $H\orbit{0,1}(c_{0})$, $H\orbit{0,1}(e_{0})$, $H\orbit{0,1}(g_{0})\}$                                                \\
				\hline
				\centering $\orbit{1,2}$
		                         & Création $H\orbit{1,2}(a_{0})$
				\par Modification~$H\orbit{1,2}(b_{0})$\ldots
				\vspace{0.1 cm}
				\par Création~$H\orbit{1,2}(a_{2})$
		                         &
		                         &                                                                                                                                                                                        \\
		\hline
	\end{tabular}
	\normalsize

\end{table*}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../../../main"
%%% End:
