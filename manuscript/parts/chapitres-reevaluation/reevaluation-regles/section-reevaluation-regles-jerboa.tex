\section[Réévaluation]{Réévaluation}
\label{sec:reevaluation-base-regles-reevaluation}

Afin de proposer un mécanisme d'appariement pour la réévaluation, nous nous
appuyons à nouveau sur les travaux de Cardot. %
Dans ses travaux, elle procède à l'appariement à partir des journaux
d'historiques qu'elle propose de mettre à jour dans des DAGs (voir
figure~\ref{fig:etat-art-modelisation-parametrique-synthese-cardot-journal-historique}
page~\pageref{fig:etat-art-modelisation-parametrique-synthese-cardot-journal-historique}
et
figure~\ref{fig:etat-art-modelisation-parametrique-synthese-cardot-arbre-appariement}
page~\pageref{fig:etat-art-modelisation-parametrique-synthese-cardot-arbre-appariement}). %
Notre approche est très similaire dans le sens où nous effectuons l'appariement
progressivement au cours de la réévaluation grâce à la mise à jour de nos DAGs
d'évaluations. %
La différence entre nos deux approches se trouve, d'une part, dans le niveau de
détail proposé par nos DAGs et, d'autre part, par la possibilité de les
exploiter pour mettre en \oe uvre des stratégies de réévaluation qui sont
configurables par l'utilisateur. %

% Commençons avec la création des DAGs de réévaluations. %

\subsection{Reconstruction à partir d'un DAG d'évaluation}
\label{sec:reevaluation-base-regles-reevaluation-reconstruction}

Comme nous l'avons exprimé plus tôt, la construction d'un DAG de réévaluation
consiste simplement à mettre à jour un DAG d'évaluation, étape par étape, en
prenant en compte la nature (ou \emph{type}) des applications, c'est-à-dire
\texttt{INIT} si elles viennent de l'évaluation \textbf{initiale} sinon
\texttt{ADD}, \texttt{DELETE} ou encore \texttt{MOVE} si elles sont
\textbf{ajoutées}, \textbf{supprimées} ou \textbf{déplacées} dans la
spécification paramétrique éditée. %
Enfin, et à l'inverse de la construction d'un DAG d'évaluation, un DAG de
réévaluation est construit dans l'ordre d'application des règles. %

Lors de l'appariement, l'objectif est d'identifier explicitement les orbites par
un brin et non pas par des n\oe uds de règles Jerboa qui peuvent filtrer
plusieurs instances de brins. %
Les DAGs de réévaluation n'enregistrent donc pas des n\oe uds de règles Jerboa
mais des brins. %

Ainsi, la méthode consiste, pour chaque application d'un type donné, à~:
\begin{description}
	\item[\texttt{INIT}~:] mettre à jour les évènements initialement détectés
	      lorsque nécessaire~;
	\item[\texttt{ADD}~:] intégrer des niveaux d'applications supplémentaires~;
	\item[\texttt{DELETE}~:] gérer l'annulation des évènements initialement détectés, voire interrompre l'appariement~;
	\item[\texttt{MOVE}~:] gérer les conséquences d'un déplacement, c'est-à-dire si
	      l'opération déplacée ou les suivantes sont toujours applicables. %
\end{description}

Prenons à nouveau l'application \texttt{8-colorationBleu} comme point de départ
pour illustrer la création d'un DAG de réévaluation et les différentes étapes
d'appariement des paramètres topologiques. %
% À l'aide du DAG d'évaluation que nous avons construit plus tôt
% (figure~\ref{fig:reevaluation-base-regle-nomination-persistante-orbites-construction-dag-etape-4}),
% nous pouvons identifier l'ensemble des orbites qui correspondent, du point de
% vue de leurs constructions respectives, à l'orbite enregistrée comme paramètre
% topologique dans la spécification paramétrique. %


\begin{figure}[htb!]
	% \centering
	\begin{subfigure}{.19\textwidth}
		\centering
		\includegraphics[width=.9\textwidth]{1-reevaluation-square}
		\caption{}
		\label{fig:reevaluation-base-regle-reevaluation-pn35-step-1-appariement}
	\end{subfigure}
	\hfill
	\begin{subfigure}{.79\textwidth}
		\raggedleft
		\includegraphics[scale=.455]{reevaluationTree-6-export-step-1}
		\caption{}
		\label{fig:reevaluation-base-regle-reevaluation-pn35-step-1-dag}

	\end{subfigure}
	\caption{Appariement des orbites issues de \texttt{INIT 1-carre()}}
	\label{fig:reevaluation-base-regle-reevaluation-pn35-step-1}
\end{figure}

La première étape d'appariement concerne l'application \texttt{INIT
	1-carre}
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-1}). %
Pour commencer, nous vérifions que la règle est toujours applicable. %
Puisque \texttt{1-carre} ne référence aucun paramètre topologique, elle est
immédiatement ré-applicable. %
Logiquement, comme nous l'avons démontré dans le
chapitre~\ref{cha:formalisation-evenements}
(page~\pageref{cha:formalisation-evenements}), les évènements de création sont
produits pour toute application sur tout objet. %

Nous pouvons alors reproduire le premier niveau d'application du DAG
d'évaluation qui contient deux n\oe uds d'évènements \creationnodeevent{} et
deux n\oe uds d'orbite brin \orbitnode{\(\orbit{}\)} et arête
\orbitnode{\(\orbit{0}\)} incidentes au n\oe ud \(n_{3}\). %
Comme évoqué plus tôt, dans un DAG de réévaluation les niveaux d'orbites sont
annotés par des brins et non des n\oe uds. %
Pour cela, nous récupérons un brin parmi ceux filtrés par le n\oe ud \(n_{0}\)
dans l'application \texttt{1-carre}. %
Puisque \(n_{3}\) ne crée qu'un unique brin \(3\)
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-1-appariement}),
c'est ce brin que nous enregistrons dans le DAG de réévaluation
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-1-dag}). %

\begin{figure}[htb!]
	\centering
	\begin{subfigure}{.19\textwidth}
		\centering
		\includegraphics[width=.9\textwidth]{1ADD-reevaluation-insert}
		\caption{\todo{refaire la figure pour afficher les brins 3 et 4}}
		\label{fig:reevaluation-base-regle-reevaluation-pn35-step-2-appariement}
	\end{subfigure}
	% \hspace{0.06\textwidth}
	\hfill
	\begin{subfigure}{.79\textwidth}
		\raggedleft
		\includegraphics[scale=.455]{reevaluationTree-6-export-step-2}
		\caption{}
		\label{fig:reevaluation-base-regle-reevaluation-pn35-step-2-dag}
	\end{subfigure}
	\caption{Appariement des orbites issues de \texttt{ADD 9-insertion}}
	\label{fig:reevaluation-base-regle-reevaluation-pn35-step-2}
\end{figure}

L'édition de la spécification paramétrique ajoute l'application \texttt{ADD
	9-insertion(3)}
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-2}). %
Puisqu'elle est ajoutée, nous vérifions tout d'abord si elle a un impact sur les
évolutions d'orbites représentées dans ce DAG. %
Pour cela, nous vérifions si la règle filtre tout ou partie d'orbite incidente
au brin \(3\) enregistré dans le DAG. %
Si ce n'est pas le cas niveau d'application inséré est une simple copie du
niveau précédent dans lequel les n\oe uds d'évènements sont tous remplacés par
des \noeffectnodeevent{}. %

Dans notre exemple,  l'application désigne l'arête incidente au  brin \(3\) donc
nous poursuivons avec la détection des évènements. %
L'insertion d'un sommet sur l'arête incidente au brin \(3\) la scinde en deux. %
La détection d'une scission, provoquée  par une application ajoutée, provoque le
dédoublement  de la  branche de  suivi pour  suivre l'évolution  de chacune  des
entités issues de la scission. %
Nous avons alors  deux branches de suivi avec chacune  un niveau d'évènement qui
contient  un  n\oe  ud  d'évènement   \nomodifnodeevent{},  car  les  brins  aux
extrémités   de   l'arête   sont   inchangés,  et   un   n\oe   ud   d'évènement
\splitnodeevent{},    car    l'arête    est    scindée    en    deux    (figure~
\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-2-appariement} ). %
De même, chaque branche contient un niveau d'orbites avec les n\oe uds d'orbites
\orbitnode{\(\orbit{}\)}  et \orbitnode{\(\orbit{0}\)}  et sont  distinguées par
leurs brins respectifs. %
En effet, une branche suit désormais  l'évolution des orbites incidentes au brin
\(3\)  tandis   que  l'autre   suit  les  orbites   incidentes  au   brin  \(4\)
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-2-dag}). %

\begin{figure}[htb!]
	\centering
	\begin{subfigure}{.19\textwidth}
		\centering
		\includegraphics[width=.9\textwidth]{2-reevaluation-extrusion}
		\caption{\todo{refaire la figure pour afficher les brins 37 et 52}}
		\label{fig:reevaluation-base-regle-reevaluation-pn35-step-3-appariement}
	\end{subfigure}
	\hfill
	\begin{subfigure}{.79\textwidth}
		\raggedleft
		\includegraphics[scale=.455]{reevaluationTree-6-export-step-3}
		\caption{}
		\label{fig:reevaluation-base-regle-reevaluation-pn35-step-3-dag}
	\end{subfigure}
	\caption{Appariement des orbites issues de \texttt{INIT 2-extrusionFace}}
	\label{fig:reevaluation-base-regle-reevaluation-pn35-step-3}
\end{figure}

Suite à la scission ajoutée à l'étape précédente, nous suivons l'impact de
l'application \texttt{INIT 2-extrusionFace} pour chaque branche du DAG
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-3}). %
Comme prévu initialement, l'application de la règle crée des arêtes par
extrusion de brins et des faces par extrusion d'arêtes. %
Nous enregistrons donc deux n\oe uds d'évènements \creationnodeevent et deux
n\oe uds d'orbites \orbitnode{\(0\)} et \orbitnode{\(0,1\)} dans chaque branche
du DAG
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-3-dag}). %
Le n\oe ud de règle initialement enregistré, \(n_{4}\), crée un ensemble de
brins, mais seuls deux d'entre eux sont directement issus des brins \(3\) et
\(4\). %
Ce sont respectivement les brins \(37\) et \(52\)
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-3-appariement}),
que nous enregistrons dans les niveaux d'orbites du DAG. %

\begin{figure}[htb!]
	\centering
	\begin{subfigure}{.19\textwidth}
		\centering
		\includegraphics[width=.9\textwidth]{5-reevaluation-triangulation}
		\caption{\todo{refaire la figure pour afficher les brins 37 et 52}}
		\label{fig:reevaluation-base-regle-reevaluation-pn35-step-4-appariement}
	\end{subfigure}
	\hfill
	\begin{subfigure}{.79\textwidth}
		\raggedleft
		\includegraphics[scale=.455]{reevaluationTree-6-export}
		\caption{}
		\label{fig:reevaluation-base-regle-reevaluation-pn35-dag}
	\end{subfigure}
	\caption{Appariement des orbites issues de \texttt{INIT 5-triangulation}}
	\label{fig:reevaluation-base-regle-reevaluation-pn35}
\end{figure}

La dernière étape d'appariement du PN\textsubscript{35} concerne l'application
\texttt{INIT 5-triangulation}
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35}). %
Lors de cette scission, les faces suivie dans chacune des branches sont
scindées, mais puisque cet évènement était prévu dans l'évaluation initiale,
aucun dédoublement de branche n'est nécessaire. %
Nous enregistrons donc un n\oe ud d'évènement \splitnodeevent{} ainsi qu'un n\oe
ud d'orbite \orbitnode{\(0,1\)} dans les niveaux d'évènements et d'orbites de
chaque branche
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-dag}). %
Dans cette règle, les brins \(37\) et \(52\) ont été filtrés et préservés par le
n\oe ud \(n0\) de la règle de triangulation. %
Ainsi, nous enregistrons ces deux brins dans les niveaux d'orbites du DAG. %

À ce stade de la réévaluation, la procédure d'appariement pour le
paramètre topologique de l'application \texttt{INIT
	8-coloration(PN\textsubscript{35}, Bleu)} est terminée. %
Le DAG de réévaluation indique que deux orbites peuvent être sélectionnées par
l'utilisateur pour la réévaluation~: les faces \(\orbit{0,1}(37)\) et
\(\orbit{0,1}(52)\)
(figure~\ref{fig:reevaluation-base-regle-reevaluation-pn35-step-4-appariement}). %



\subsection{Stratégies de réévaluation}
\label{sec:reevaluation-base-regles-reevaluation-strategies}

\begin{itemize}
	\item Définir stratégie (interactive et pré-définie)~;
	\item exploiter les DAGs pour définir des stratégies diverses et variées~;
	\item exemple scission ajoutée `1 ou tout ou cas par cas` en reprenant la coloration bleue~;
	\item exemple annulation d'évènement `fusion ou non` de deux entités qui n'en
	      forment plus qu'une~;
	\item exemple suppression `interruption appariement` lorsqu'un paramètre
	      topologique n'est plus créé. %

\end{itemize}

% \begin{itemize}
% 	\item Scission d'un paramètre topologique : évaluation de tout ou partie des paramètres éligibles
% 	\item Sélection d'une branche prédominante en cas de fusion
% 	\item Suppression d'un paramètre topologique : interruption de la réévaluation pour ce paramètre (potentiellement de l'objet)
% \end{itemize}

% \maxime{Dans cette section, expliquer que l'on peut, à l'aide de ces DAGs,
% 	mettre en \oe uvre des stratégies pour la réévaluation.} \maxime{Expliciter le
% 	terme de stratégie, c'est-à-dire, lorsqu'il y a plusieurs branches (dues à des
% 	scissions ajoutées par l'édition de la spec), on peut proposer à l'utilisateur
% 	de choisir parmi un ensemble de branches pour la réévaluation. %
% 	D'autres stratégies sont possibles, comme (lors d'une fusion ajoutée ou une
% 	annulation de scission) choisir qu'elle branche, parmi celles fusionnées, est
% 	prédominante.}

% \maxime{Je ne vois trop ce qui pourrait être ajouté, certainement des
% 	illustrations}

% \maxime{1. illustration stratégie scission rééval branche 1\\
% 	2. stratégie scission rééval branch 2\\
% 	3. stratégie suppression arrêt rééval}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: "../../../main"
%%% End:
